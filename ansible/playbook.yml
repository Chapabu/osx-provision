- hosts: localhost
  connection: local
  vars_files:
    - "vars/local_vars.yml"

  roles:
    - { role: homebrew, tags: ["php", "mysql", "apache", "apps"]}
    - mysql
    - php
    - apache
    - apps