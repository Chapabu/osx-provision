- hosts: localhost
  connection: local

  roles:
    - { role: homebrew, tags: ["php", "mysql", "apache", "casks", "nodejs", "vim", "zsh"]}
    - mysql
    - php
    - apache
    - casks
    - nodejs
    - zsh
