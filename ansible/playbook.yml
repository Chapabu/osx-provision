- hosts: localhost
  connection: local
  vars_files:
    - "vars/local_vars.yml"

  roles:
    - { role: homebrew, tags: ["php", "mysql", "apache", "cask", "nodejs"]}
    - mysql
    - php
    - apache
    - cask
    - nodejs