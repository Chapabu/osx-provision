- hosts: localhost
  connection: local
  vars_files:
    - "vars/local_vars.yml"

  roles:
    - homebrew
    - mysql

  tasks:
    - name: Get Brew prefix
      shell: echo $(brew --prefix)
      register: brew_prefix
      when: brew_prefix is not defined